enum SPTypes { string, int, double, bool }

enum Roles { user, admin }

enum TextLabelSizes { small, medium, large }

enum TextLabelColors { black, blue, white }

enum TextLabelFontStyle { style1, style2, style3 }
